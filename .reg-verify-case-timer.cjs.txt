[run-e2e] 起 vite @ http://127.0.0.1:5200
[run-e2e] vite 就绪，开始跑 .verify-case-timer.cjs

[A] 首页顶栏（variant=simple）不显示「操作用时」卡
  ✓ 首页无 .hd-time-card（未显示操作用时）

[B] 从首页点「案例学习」进入 → 用时从 00:00 开始走
  ✓ 进入 /case-study
  ✓ 存储：计时器 running=true（已开始计时）
  ✓ 进入后用时从 00:00 附近起算
  ✓ 等待 3.5s 后用时已走 ≥ 3 秒（确实在走）

[C] 进入模块 → 计时连续累计（不重置）
  ✓ 进入 /analysis
  ✓ 进入模块后用时继续增长（未归零）
  ✓ 存储：仍 running=true

[D] 从模块返回案例学习页 → 继续累计
  ✓ 回到案例学习页后用时继续增长

[E] 返回首页 → 用时**停止**
  ✓ 返回首页 /
  ✓ 存储：running=false（已停止）
  ✓ 停止时已用时长被保留（> 0）
  ✓ 静置 3s 后 accumulatedMs 未增长（确实停了）

[F] 再次从首页点「案例学习」进入 → 用时**归零**重新计时
  ✓ 再次进入案例学习
  ✓ 再次进入后重新开始计时（running=true）
  ✓ 用时已归零（≤ 1 秒）
  ✓ 归零后重新开始走（2s 后约 2 秒，而非接着旧的几十秒）

[G] 案例学习内刷新页面（F5）→ 接着已用时长继续
  ✓ 刷新后仍在计时（running=true）
  ✓ 刷新后用时接着（未归零）

================ 汇总 ================
通过 19 项，失败 0 项
[run-e2e] .verify-case-timer.cjs 退出码 0，已收掉 vite
